// Ambient, app-wide type declarations only. API DTOs are generated
// (`@/service/models`); feature-local UI types live in their feature.
// Intentionally empty at Phase 0 — add ambient globals here as they appear.

export {};
