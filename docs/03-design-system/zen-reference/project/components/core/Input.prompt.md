Standard text field; 36px tall, hairline border, 3px orange focus ring.

```jsx
<Input label="Email" type="email" placeholder="you@example.com" />
<Input label="Passphrase" type="password" hint="Never stored — in-memory only." />
<Input label="Name" error="A file with this name already exists." />
```
