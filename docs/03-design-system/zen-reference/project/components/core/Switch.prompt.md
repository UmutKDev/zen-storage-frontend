On/off toggle for settings (e.g. two-factor, hidden-folder reveal). Orange when on.

```jsx
<Switch defaultChecked aria-label="Two-factor authentication" />
<Switch checked={on} onCheckedChange={setOn} />
```
