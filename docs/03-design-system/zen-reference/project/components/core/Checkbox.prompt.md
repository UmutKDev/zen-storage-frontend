Selection checkbox; the 16px box extends to a ≥40px hit target automatically. Inside FileRow/FileCard it fades in on hover.

```jsx
<Checkbox aria-label="Select Documents" checked={selected} onCheckedChange={toggle} />
```
